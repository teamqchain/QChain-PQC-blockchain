# macOS / OS noise
.DS_Store

# Editors and IDEs
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*.swo
*~

# Logs and runtime dumps
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment and local overrides
.env
.env.*
*.local

# Node / JavaScript
**/node_modules/
**/dist/
**/coverage/
*.tsbuildinfo

# Python
**/__pycache__/
**/*.py[cod]
.venv/
venv/

# Go
*.test
*.out
coverage.out

# Dart / Flutter
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/.packages
**/build/

# Project-specific generated artifacts and secrets
qchain-network/crypto-config/
qchain-network/organizations/
qchain-network/crypto-material/
qchain-network/wallet/
qchain-network/keystore/
qchain-network/channel-artifacts/*.block
qchain-network/channel-artifacts/*.tx
qchain-network/channel-artifacts/*.pb
qchain-network/channel-artifacts/*.json
qchain-network/channel-artifacts/*.tar.gz
qchain-network/channel-artifacts/*.tar
qchain-network/channel-artifacts/*.zip

# Docker overrides
docker-compose.override.yml
docker-compose.*.override.yml
docker-compose.*.override.yaml

# VM runtime binaries (downloaded on the VM, not for the repo)
cloudflared
backend/
offchain/kubo/
offchain/upload-results/

# Phase 2 Fabric source clone (kept on VM, not committed)
fabric/

# Fabric gateway connection profiles (contain env-specific TLS certs)
# Copy connection-*.example.json to connection-*.json and fill in your certs
qchain-network/connection/connection-gen.json
qchain-network/connection/connection-gov.json

# npm lock file generated on VM
qchain-network/scripts/package-lock.json